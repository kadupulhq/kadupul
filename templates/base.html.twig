<!DOCTYPE html>
<html lang="{{ (app.request ? app.request.locale : 'en')|replace({'_': '-'}) }}">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}Kadupul{% endblock %}</title>
    </head>
    <body>{% block body %}{% endblock %}</body>
</html>
