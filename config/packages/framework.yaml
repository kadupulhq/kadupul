framework:
    secret: '%env(APP_SECRET)%'
    http_method_override: false
    handle_all_throwables: true
    router:
        utf8: true
    php_errors:
        log: true
    # Authentication and legacy session bridging are a later migration step.
    session: false

when@test:
    framework:
        test: true
