{
    "_comment": "Test-runner sandbox. The application's main composer.json uses vendor-dir=include/vendor/; Pest's directory walk does not find composer.json from there. Scoping Pest to tests/ keeps the main tree untouched and gives Pest a composer.json it can discover.",
    "name": "kadupulhq/tests",
    "description": "Test-runner deps for the CSP nonce migration suite.",
    "type": "project",
    "require-dev": {
        "pestphp/pest": "^4.7",
        "nikic/php-parser": "^5.0"
    },
    "config": {
        "platform": {
            "php": "8.4.0"
        },
        "allow-plugins": {
            "pestphp/pest-plugin": true
        }
    },
    "minimum-stability": "stable"
}
