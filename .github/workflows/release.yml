name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: read

jobs:
  release:
    uses: kadupulhq/.github/.github/workflows/release.yml@main
    permissions:
      contents: write
      packages: write
    with:
      tarball: true
      image: true
      php-version: '8.3'
